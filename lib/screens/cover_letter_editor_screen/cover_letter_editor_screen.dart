import 'package:flutter/material.dart';

class CoverLetterEditor extends StatelessWidget {
  static const String id = 'cover_letter_editor_screen';
  const CoverLetterEditor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('CoverLetterEditorScreen'),
    );
  }
}
